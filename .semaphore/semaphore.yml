version: v1.0
name: cc-billing-worker
agent:
  machine:
    type: s1-prod-ubuntu20-04-amd64-2
execution_time_limit:
  minutes: 90
global_job_config:
  env_vars:
    - name: SEMAPHORE_TRIGGER_SOURCE
      value: push
  prologue:
    commands:
      - checkout
blocks:
  - name: "Tests"
    dependencies: []
    task:
      jobs:
        - name: "Tests"
          commands:
            - tox
